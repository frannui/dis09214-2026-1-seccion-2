# sesión 09 - 05/06  
## Estados

### Switch  
paso 5: Para pasar de un estado a otro y lograr que vuelva al comienzo usamos una interación ej: `moussePressed();` cada vez que hagas click la variable aumenta. Si llega a 3 despues del estado 2 la avariable vuelve a 0.  

### interaccion con el teclado  
1.1. Por barra espaciadora:  
# Interación con el mundo físico  
### Cámara Web  
1.- Crear varianble para captura de la cámara que guardara el flujo del video.  
2.- inicializacom en el setup y usamos `createCapture(VIDEO)`. Definimos el tamaño de la captura de la cámara con `captura.size(x,y)` usar el `captura.hide();` para que esconda el video que p5js coloca por defecto.  
3.- Dibujar la cámara en el `draw()` usamos la function (image), toma cada cuadro (frame) de la cámara y lo dibuja a tiempo real.  




      

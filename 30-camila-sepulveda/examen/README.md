# Examen: Tamara Barberan, Camila Sepúlveda.
¿Qué es el proyecto?
●

Este trabajo habla sobre una de las problemáticas de género más grande a la
cual muy pocas veces se le da visibilidad, es una representación abstracta de
la violencia psicológica de género contra la mujer, utilizando diversas formas
geométricas y figuras que aluden a esta problemática.
¿Qué se ve en pantalla?
●
En la pantalla de inicio se observa un fondo negro acompañado de dos
jerarquiaz de texto, la mas grande de color rojo que dice “violencia psicologica”
y un texto secundario que menciona “preciona espacio para comenzar”de color
blanco. Por otro lado la pantalla de instrucción se observa una textura de fondo, sobre ella
hay 4 ojos en el centro del canvas, que están acompañado de cuatro jerarquías
de texto. la primera que dice “¿Te han hecho esto?” de color rojo, la segunda
jerarquia “instrucciones” de color blanco, la siguiente “nadie te va a querer”,
“es tu culpa”, “no sirves para nada”, de color rojo. La ultima jerarquia es de color
blanco que dice “mueve el mouse para interactuar con el ojo”, “mantén presionado
para revelar la violencia”, “habla al micrófono en la última escena” y “presiona ENTER para
continuar”.
En la primera pantalla en el centro se ve un corazón rojo que predominan
visualmente en la pantalla, en el fondo se aprecia
una textura oscura, acompañado de circulos negros en movimiento, además también
en el centro se distingue la silueta negra de una mujer, a su costados se observan
manos rojas/negro que se dirigen a los elementos del centro, también se puede observar un
ojo con
líneas rojas originadas desde la pupila del ojo dispersando hacia la parte
superior del lienzo. Además tiene texto blanco en diversas jerarquías, la más predominante
que dice “no más violencia” y “heridas que no se ven”, el secundario “la violencia
psicológica también deja marcas” y por último un texto en rojo que dice “cambiando en:
segundos”.
En la segunda pantalla hay un fondo completamente rojo, que esta acompañado de una
imagenen negro de flor sobre una grieta, la flor esta rodeada en la parte superior de texto
blanco que dice “rebelate” repetido 3 veces y abajo en la zona de la grieta de la flor hay
textos en negro de una jerarquia mas pequeña que dice “anda a la cocina”, “callate”,
“estupida”, “no llori tanto”, “te vas a arrepentir”, “loca”, “inutil”, “no sirves para nada”.
Por otra parte está presente la webCam, con la activación de la cámara, se ve lo que está a
través del dispositivo. En la cámara se ven píxeles que están en constante cambio. Por otra
parte en la zona superior se ve un texto blanco que dice “Habla al micrófono para
transformar tu imagen”.
Para finalizar como tercera pantalla se ve presente un fondo blanco que abarca toda la
pantalla, en el centro una imagen de una flor gris sobre una grieta, sobre ella un texto rojo
en grande que dice “tu voz importa” y debajo de la flor otros que dice en negro “denuciar es
un acto de valentia no estas sola” y como ultima jerarquía en texto rojo “haz click para
sembrar nuevas voces”. Por último al hacer click en la pantalla se generan flores de
pequeña escala en donde se vaya presionando el mouse.

¿Qué elementos visuales aparecen?
●
Predominan elementos de silueta como tipo de imagen utilizada, por otro lado predomina el
uso de texturas
como fondo, los colores negro, rojo, gris y blanco y el uso de jerarquias tipograficas como
elemento importante de apoyo de contenido.
Descripción conceptual
●
La obra representa la violencia de género desde una perspectiva psicológica
y abstracta, el uso de sombras, distorsión y colores rojos simbolizan miedo,
control y ansiedad, el ojo representa vigilancia y presión constante, las líneas
y movimientos y texturas generan incomodidad visual para transmitir tensión
emocional, el texto blanco actúa como intermediaria dentro de la situación
de tensión, llevándose a cabo en el uso de texto. El negro da intención
al vacío y a la oscuridad que siente la persona, el color gris es para generar tensión
y limbo entre la oscuridad y el querer surgir hacia la luz y
la interacción del usuario simboliza cómo la violencia altera el estado
emocional y psicológico de la víctima.
●
Este trabajo sigue una lógica de sistema que juega con la mirada figurativa y
literal, resaltando el color rojo, negro, gris y blanco y predominando las imagenes
cómo silueta, al ser algo menos claro y el uso de texto. En el sketch se enfatiza
esta atmósfera sombría y tensa, contando una historia de recorrido en donde te hace
convivir en el contexto y narrativa del acto de violencia, para adentrarte más en su
contenido que va en constante surgimiento para el progreso.
El propósito de la pantalla de inicio es introducir y explicar el tema, para luego continuar con
la pantalla de introducciones, que es para conectar y da a entender al observador las
interacciones dentro del sketch, para sentirse más orientado y dentro del discurso.
En la pantalla uno el corazón se entiende como el sentir de la persona cuando sufre
violencia psicológica, late rápido, se expande, se siente pesado, como cuando se te aprieta
el corazón, además hace un juego con los colores, representando el cambio anímico ante
una situación de tensión.
En la pantalla dos las palabras se asocian con las grietas que abarcan el contenido de
violencia, estas tiemblan porque son palabras que reciben las víctimas y hacen temblar de
angustia y de miedo. Estas al alejarse y separarse de la flor, dan a entender que la flor en
este caso la víctima, renace y sana de toda esa violencia que sufrió.
Por otro lado la pantalla tres es para que los usuarios se pongan en los zapatos de la
víctima, en este caso los píxeles que aparecen al haber ruido, significan los gritos y las
palabras negativas que reciben la víctima que se buscan liberar de alguna manera.

Por último para la pantalla final son palabras de aliento para las víctimas o los usuarios que
son víctimas de violencia psicológica denuncien y puedan alzar la voz o para poder ayudar
a personas que necesiten de un apoyo. por eso la frase “haz click para sembrar voces”.
¿Cómo se relaciona esta lógica con la problemática de género elegida?●
Se relaciona, ya que, juega con elementos y colores que enfatizan la
violencia psicológica de género,nos hace comprender y habitar la situación
desde la mirada de la víctima.
• Input / Output y sistema
●
El proyecto funciona a partir de la interacción del usuario con el mouse. Los
datos de entrada o inputs corresponden principalmente a la posición del
cursor en la pantalla y a las acciones de presionar o mantener presionado el
mouse. Estos datos son captados constantemente por el sistema mediante
variables de p5.js como mouseX, mouseY y mouseIsPressed.
●
Una vez recibidos estos datos, el programa los procesa y transforma en
distintos cambios visuales. El movimiento del mouse modifica la intensidad
del color rojo, altera la dirección de las líneas y mueve el ojo que sigue al
usuario,aumenta la escala de elementos, aparecen elementos nuevos.
Además, las figuras geométricas rotan y cambian visualmente
mediante funciones como rotate(), translate() y scale(), generando una
sensación de inestabilidad y tensión psicológica. También se utilizan
funciones como random() para crear sombras y partículas aleatorias que
aportan caos visual y refuerzan la atmósfera inquietante de la obra.
●
Como resultado, el sistema produce una respuesta visual abstracta e
interactiva que representa emociones relacionadas con la violencia de
género, especialmente la violencia psicológica. La combinación de
movimiento, color, distorsión y superposición de imágenes busca generar
incomodidad visual y transmitir sensaciones de miedo, vigilancia, ansiedad y
encierro emocional.
Pensamiento computacional
●
El sistema está construido a partir de reglas programadas que relacionan
directamente las acciones del usuario con cambios visuales dentro de la
composición. El programa recibe datos de entrada mediante el mouse,
procesa esos datos utilizando variables, funciones, condicionales y bucles, y
finalmente genera una respuesta gráfica en tiempo real. Por ejemplo,
dependiendo de la posición del cursor, el sistema modifica mensajes en
pantalla, altera colores y dirige las líneas visuales hacia el movimiento del
usuario.
●
La interactividad es uno de los elementos centrales de la obra, ya que el
espectador no solo observa la composición, sino que también la modifica
constantemente con sus acciones. Cuando el usuario mueve el mouse, el ojo

sigue sus movimientos, el texto o las flores y las líneas cambian de dirección, creando la
sensación de vigilancia, presión psicológica o cambio de estado. Al mantener presionado el
mouse,
aparecen imágenes relacionadas con la violencia y la tensión
emocional, intensificando la experiencia visual. De esta manera, la obra
utiliza programación e interacción digital para representar cómo la violencia
psicológica altera constantemente la percepción y el estado emocional de
una persona.
Referentes
●
Los referentes visuales del proyecto provienen principalmente de posters que
abordan esta problemática, principalmente de un poster de la Facultad de
Ciencias Culturales (FIB) de la Universidad Diponegoro (Undip) en
Indonesia,fue una pieza de comunicación visual creada por los estudiantes y
la facultad para concientizar y luchar contra la violencia y el acoso sexual
dentro del campus universitario.
●
En general fue ese poster que me dio la iniciativa para crear mi obra y la
principal inspiración, ya que me atrajo demasiado que el poster jugará con
colores limitados, especialmente el uso del rojo, negro y sombras para
transmitir tensión, peligro y ansiedad.

Código p5.js:
```javascript
// VARIABLES PROPIAS
let captura;// variable que habilita el uso de cámara
let mic; // variable que habilita el microfono
let estado = 0; // controla las pantallas
let floresExtras = [];
let frases = []; // array solicitado por el examen
let tiempoInicioEstado = 0; // Guardará el milisegundo exacto en que cambia el estado
let duracionEstado = 9000; // 9000 milisegundos = 9 segundos
let flor;//variable para guardar la imagen png de flor
let mujer; // variable para guardar la imagen mujer
let corazon; // variable para guardar la imagen corazon
let violencia; // variable para guardar la imagen violencia
let ojo; // variable para guardar la imagen del ojo
let textura; // variable para guardar la textura del fondo
let rotacion = 0; // controla la rotación de las figuras
let tamanoCirculo = 120; // tamaño del círculo central
let colorRojo = 0; // controla intensidad del color rojo
let mensaje = &quot;Silencio&quot;; // texto que aparece en pantalla
let fuenteInicio; // variable para guardar la tipografia del inicio
let fuenteTexto; // variable para guardar la tipografia Inter_Regular

let musica; // variable para guardar musica de tension mp3
// BOTÓN REINICIAR
let botonX; // posición horizontal del botón para reiniciar
let botonY; // posición vertical del botón para reiniciar
let tamBoton = 70; // tamaño inicial del corazón
// CLASS
class Particula {
constructor() { // construye cada partícula asignando posición y tamaño aleatorio
this.x = random(width);
this.y = random(height);
this.tam = random(5, 15);
}
mostrar() { // construye cada partícula asignando posición y tamaño aleatorio
fill(255, 0, 0, 100);
noStroke();
circle(this.x, this.y, this.tam);
}
}
// FUNCTION PRELOAD
function preload() { // // carga imágenes, fuentes y sonidos antes de iniciar el sketch en el
canvas
ojo = loadImage(&quot;ojo.png.png&quot;); // carga la imagen del ojo
textura = loadImage(&quot;textura.jpg.jfif&quot;); // carga la textura de fondo
violencia = loadImage(&quot;violencia.png&quot;); // carga la imagen de la mujer
mujer = loadImage(&quot;mujer.png&quot;); // carga la imagen de la mujer
flor = loadImage(&quot;flor.png&quot;);// carga la imagen de la flor
corazon = loadImage(&quot;corazon.png&quot;); // carga la imagen CORAZÓN
fuenteInicio = loadFont(&quot;BebasNeue-Regular.ttf&quot;); // carga la tipografía BebasNeue-Regular
musica = loadSound(&quot;tension.mp3&quot;); // carga la musica de tension
fuenteTexto = loadFont(&quot;Inter_Regular.ttf&quot;); // carga tipografía legible para textos pequeños
}
// FUNCTION SETUP
function setup() { // carga imágenes, fuentes y sonidos antes de iniciar el sketch
createCanvas(windowWidth, windowHeight); // crea un canvas adaptable al tamaño de la
ventana
textAlign(CENTER, CENTER); // centra el texto en el medio del canvas
textFont(fuenteInicio); // aplica Bebas Neue a todos los textos del proyecto
captura = createCapture(VIDEO);// activa la webcam dentro del canvas
captura.size(width, height); // ajusta el tamaño de la webcam al canvas

captura.hide(); // ajusta el tamaño de la webcam al canvas
mic = new p5.AudioIn();// activa la opción de utilizar el audio dentro de la cámara
mic.start();// inicia la utilización del micrófono
noStroke(); // desactiva los contornos en los elementos
for (let i = 0; i &lt; 20; i++) { // crea 20 partículas automáticamente
frases.push(new Particula());// Crea una nueva partícula y la agrega al final del arreglo
llamado &quot;frases&quot;
}
userStartAudio(); // habilita el sonido en navegadores modernos
}
// FUNCTION DRAW
function draw() { // función de dibujo, para que aparezcan los elementos dentro del canvas
let tiempoTranscurrido = millis() - tiempoInicioEstado; // se configua variable para el
tiempo de inicio transcurrido dentro del canvas
switch (estado) { // función principal que se ejecuta continuamente
case 0:
pantallaInicio();
break;
case 1:
pantallaInstrucciones();
break;
case 2:
pantallaUno(tiempoTranscurrido);
break;
case 3:
pantallaDos(tiempoTranscurrido);
break;
case 4:
pantallaTres(tiempoTranscurrido);
break;
case 5:
pantallaFinal();
break;
}
// temporizador automático entre pantallas
if (tiempoTranscurrido &gt; duracionEstado &amp;&amp; estado &lt; 5) {
estado = estado + 1; // avanza solo hasta la pantalla final
tiempoInicioEstado = millis(); // reinicia el tiempo
}
}
// PANTALLAS

function pantallaInicio() { // muestra la portada principal del proyecto
background(0);
fill(255,0,0);
textFont(fuenteInicio); // aplica una tipografía específica
textSize(80);
text(&quot;VIOLENCIA PSICOLÓGICA&quot;, width/2, height/2-50);
textSize(20);
fill(255);
text(&quot;Presiona ESPACIO para comenzar&quot;, width/2, height/2+30); // aplica una tipografía
específica
textFont(&#39;sans-serif&#39;);
}
function pantallaInstrucciones() { // muestra instrucciones para interactuar con la experiencia
background(20);
image(textura, 0, 0, width, height); // agrega textura al fondo
fill(255);
textFont(fuenteInicio); // usa Bebas Neue para el título
textSize(30);
text(&quot;INSTRUCCIONES&quot;, width/2,350);
textFont(fuenteTexto);
textSize(18);
text(&quot;Mueve el mouse para interactuar con el ojo&quot;, width/2,400);
text(&quot;Mantén presionado para revelar la violencia&quot;, width/2,450);
text(&quot;Grita al micrófono cuando aparezcas en cámara&quot;, width/2,500);
text(&quot;Presiona ENTER para continuar&quot;, width/2,550);
tint(255, 100);
image(ojo, width*0.2, height*0.3, 100, 100);
image(ojo, width*0.8, height*0.3, 100, 100);
image(ojo, width*0.2, height*0.7, 100, 100);
image(ojo, width*0.8, height*0.7, 100, 100);
fill(255, 0, 0);
let parpadeo = sin(frameCount * 0.05) * 100 + 155; // genera un efecto de parpadeo
continuo
fill(255, 0, 0, parpadeo);
textFont(fuenteInicio);
textSize(55);
text(&quot;¿TE HAN DICHO ESTO?&quot;, width/2, height*0.10);
textFont(fuenteTexto);
textSize(25);
text(&quot;NO SIRVES PARA NADA&quot;, width*0.25, height*0.2);
text(&quot;ES TU CULPA&quot;, width*0.75, height*0.20);
text(&quot;NADIE TE VA A QUERER&quot;, width*0.5, height*0.30);
}

function pantallaUno(tiempo) { // se define la primera pantalla con su tiempo establecido de
9 segundos, calculandose el tiempo actual transcurrido dentro del estado
// MÚSICA
if (!musica.isPlaying()) {
musica.loop();
}
// FONDO E IMÁGENES
background(10, 0, 0); // fondo rojo
tint(255, 70); // agrega transparencia a la textura
let escalaTextura = max(width / textura.width, height / textura.height); // calcula la escala
necesaria para cubrir toda la pantalla sin deformar la textura
let anchoTextura = textura.width * escalaTextura; // calcula el nuevo ancho proporcional de
la textura
let altoTextura = textura.height * escalaTextura; // calcula el nuevo alto proporcional de la
textura
imageMode(CENTER); // dibuja desde el centro
image(textura, width/2, height/2, anchoTextura, altoTextura); // dibuja textura sin deformarla
imageMode(CORNER); // vuelve al modo normal
let escalaMujer = max(width / mujer.width, height / mujer.height); // calcula la escala
necesaria para cubrir la pantalla sin deformar la imagen
let anchoMujer = mujer.width * escalaMujer; // obtiene el nuevo ancho proporcional de la
imagen
let altoMujer = mujer.height * escalaMujer; // obtiene el nuevo alto proporcional de la imagen
imageMode(CENTER); // dibuja desde el centro
image(mujer, width/2, height/2, anchoMujer, altoMujer); // dibuja imagen sin deformarla
imageMode(CORNER); // vuelve al modo normal
// FUNCIÓN MAP()
colorRojo = map(mouseX, 0, width, 50, 255);// cambia el color rojo según la posición del
mouse, según X e Y
// BUCLE FOR
push(); // guarda estilos y configuraciones temporales. Genera que las interacciónes
realizadas no se desplazen a otros elementos
for (let i = 0; i &lt; width; i += 50) { // se designa volores para patrón
stroke(255, 0, 0, 180); // se asigna color rojo intenso de las líneas
strokeWeight(2.5); // se asigna grosor de las líneas
line(i, 0, mouseX, mouseY); // las líneas siguen el ojo en conjunto con el mouse con X e Y
}
pop(); // restaura los estilos originales. Genera que las interacciónes realizadas no se
desplazen a otros elementos

// FUNCIÓN PROPIA
dibujarSombras(); // función propia para ordenar los elementos
// FIGURA CENTRAL
push(); // guarda estilos y configuraciones temporales. Genera que las interacciónes
realizadas no se desplazen a otros elementos
translate(width/2, height/2); // mueve el origen al centro
rotate(rotacion); // mantiene la rotación
scale(1 + sin(frameCount * 0.02) * 0.1); // mantiene el latido
tint(colorRojo, 0, 0, 220); // cambia intensidad según mouse
imageMode(CENTER); // usa el centro de la imagen como punto de referencia
image(corazon, 0, 0, tamanoCirculo * 2, tamanoCirculo * 2.5);
pop(); // restaura estilos originales
imageMode(CORNER);// usa la esquina superior izquierda como punto de referencia
(modo por defecto)
// INTERACCIÓN MOUSE
image(ojo, mouseX - 50, mouseY - 50, 100, 100); // imagen del ojo que sigue el mouse
if (mouseIsPressed) { // detecta si el mouse está siendo presionado
if (musica.isPlaying()) {
musica.pause();
}
tint(255, 220); // agrega transparencia a las imágenes
let escalaViolencia = max(width / violencia.width, height / violencia.height); // calcula la
escala necesaria para cubrir toda la pantalla
let anchoViolencia = violencia.width * escalaViolencia; // calcula el nuevo ancho proporcional
let altoViolencia = violencia.height * escalaViolencia; // calcula el nuevo alto proporcional
imageMode(CENTER); // dibuja desde el centro
image(violencia, width/2, height/2, anchoViolencia, altoViolencia); // dibuja textura sin
deformarla
imageMode(CORNER); // vuelve al modo normal
fill(255); // cambia el color de relleno del texto
textFont(fuenteInicio); // usa Bebas Neue para el título
textSize(80); // cambia tamaño del texto
text(&quot;NO MÁS SILENCIO&quot;, width/2, height/2);
}
// CONDICIONALES
if (mouseX &lt; 300) { // condición según posición del mouse
textFont(fuenteInicio); // usa Bebas Neue para el texto

textSize(60); // cambia tamaño del texto
mensaje = &quot;HERIDAS QUE NO SE VEN&quot;; // mensaje 1
} else if (mouseX &lt; 600) { // segunda condición
textFont(fuenteInicio); // usa Bebas Neue para el texto
textSize(60); // cambia tamaño del texto
mensaje = &quot;NO MÁS VIOLENCIA&quot;; // mensaje 2
} else { // condición final
textFont(fuenteInicio); // usa Bebas Neue para el texto
textSize(60); // cambia tamaño del texto
mensaje = &quot;ROMPE EL CICLO&quot;; // mensaje 3
}
// TEXTOS
fill(255); // cambia el color de relleno del texto
textSize(40); // cambia tamaño del texto
text(mensaje, width/2, 80); // texto principal
textFont(fuenteTexto); // usa Inter para textos pequeños
textSize(18); // cambia tamaño del texto
text(&quot;LA VIOLENCIA PSICOLÓGICA TAMBIÉN DEJA MARCAS...&quot;, width/2, 550); // texto
secundario
// RANDOM()
fill(random(100,255), 0, 0, 100); // color aleatorio
noStroke();
circle(random(width), random(height), random(5,10)); // círculos aleatorios
rotacion += 0.01; // aumenta la rotación constantemente
dibujarContador(tiempo);
}
function pantallaDos(tiempo) {
let escalaPantalla = min(width / 1920, height / 1080); // calcula una escala proporcional
según el tamaño de la pantalla
background(163, 27, 7); // fondo color rojo/negro
// se inserta la imagen de la flor en el fondo
let escala = min(width / flor.width,height / flor.height); // calcula la escala necesaria para
mostrar la flor completa sin deformarla
let nuevoAncho = flor.width * escala; // calcula el nuevo ancho proporcional
let nuevoAlto = flor.height * escala; // calcula el nuevo alto proporcional
imageMode(CENTER);
image( flor,width/2,height/2,nuevoAncho,nuevoAlto);
imageMode(CORNER);
let desplazamiento = map(mouseX, 0,width,-80 * escalaPantalla, 80 * escalaPantalla); //
convierte la posición del mouse en movimiento para las frases
let temblor = random(-2,2) * escalaPantalla;
textFont(fuenteTexto);
textSize(15); // Medida para el tamaño del texto

fill(0, 0, 0); // Color negro de relleno interior del texto
textAlign(CENTER);
// Izquierda
// Izquierda
text(&quot;Debil&quot;,width*0.17 + desplazamiento + temblor,height*0.66);
text(&quot;Cállate&quot;,width*0.25 + desplazamiento + temblor,height*0.58);
text(&quot;Anda a la cocina&quot;,width*0.40 + desplazamiento + temblor,height*0.50);
text(&quot;Estúpida&quot;,width*0.38 + desplazamiento + temblor,height*0.66);
text(&quot;No llores tanto&quot;,width*0.43 + desplazamiento + temblor,height*0.77);
// Derecha
text(&quot;Te vas a arrepentir&quot;,width*0.63 - desplazamiento + temblor,height*0.50);
text(&quot;Loca&quot;,width*0.60 - desplazamiento + temblor,height*0.66);
text(&quot;Inútil&quot;,width*0.72 - desplazamiento + temblor,height*0.60);
text(&quot;No sirves para nada&quot;,width*0.76 - desplazamiento + temblor,height*0.77);
//REBELATE
textFont(fuenteInicio); // usa Bebas Neue para el texto
textSize(width*0.065);
fill (255,255, 255);
text(&quot;REBÉLATE&quot;,width*0.50,height*0.08);
text(&quot;REBÉLATE&quot;,width*0.70,height*0.22);
text(&quot;REBÉLATE&quot;,width*0.25,height*0.22);
imageMode(CORNER);
}
function pantallaTres(tiempo) {
background(0, 0, 0); // fondo negro
image(captura, 0, 0, 300, 200);
fill(255);
textFont(fuenteTexto);
textSize(30);
text(&quot;ESTO ES LO QUE SE SIENTE CUANDO TE GRITAN&quot;, width/2, 50);
// Obtiene el volumen del micrófono
let volumen = mic.getLevel();
// Convierte el volumen en un tamaño de pixel
let tamañoBase = floor(map(volumen, 0, 0.2, 10, 200, true));
// Seguridad: evita que sea 0 o inválido
if (tamañoBase &lt; 3 || isNaN(tamañoBase)) {
tamañoBase = 3;
}
captura.loadPixels(); // carga la imagen actual de la webcam
if (captura.pixels.length === 0) {

return; // espera si la cámara aún no está lista
}
// Recorremos la cámara usando el tamaño afectado por el sonido
for (let y = 0; y &lt; height; y += tamañoBase) {// Recorre la imagen de la webcam de arriba
hacia abajo
for (let x = 0; x &lt; width; x += tamañoBase) {// Recorre la imagen de izquierda a derecha
let indice = (floor(x * captura.width / width) +
floor(y * captura.height / height) * captura.width) * 4; //cuando tus ciclos for van en la
coordenada de la pantalla ej (X=300, Y=200), la computadora sabrá exactamente en qué
cajón de su memoria oculta está guardado el color de ese pedacito de tu rostro.
let r = captura.pixels[indice]; // Extrae el valor rojo del píxel
let g = captura.pixels[indice + 1]; // Extrae el valor verde del píxel
let b = captura.pixels[indice + 2];// Extrae el valor azul del píxel

let brillo = (r + g + b) / 3;// Calculamos el brillo del píxel
// El tamaño del cuadrado individual también reacciona al volumen
// Multiplicamos el brillo por el volumen para que los cuadrados &quot;tiemblen&quot; o crezcan con
el sonido
let factorSonido = map(volumen, 0, 0.4, 1, 4);// Convierte el volumen del micrófono en un
factor de crecimiento
let tamañoCuadrado = map(brillo, 0, 255, 0, tamañoBase) * factorSonido;
fill(r, g, b);// Aplica el color original de la webcam
rect(x, y, tamañoCuadrado, tamañoCuadrado); // Dibuja un cuadrado representando ese
grupo de píxeles
}
}
}
function pantallaFinal() {
background(245, 235, 240);
imageMode(CENTER);
for (let florExtra of floresExtras) {
image(flor, florExtra.x, florExtra.y, 80, 80);
}
image(flor, width/2, height/2, 500, 500);
fill(180, 0, 0);
textFont(fuenteInicio); // usa Bebas Neue para el título
textSize(80);
textAlign(CENTER);

text(&quot;TU VOZ IMPORTA&quot;, width/2, 120);
textFont(fuenteTexto); // usa Inter para textos pequeños
textSize(28);
fill(40);
text(&quot;Denunciar es un acto de valentía&quot;, width/2, height-200);
text(&quot;No estás sola&quot;, width/2, height-155);
fill(180,0,0);
textSize(20);
text(&quot;Haz click para sembrar nuevas voces&quot;, width/2, height-115);
// BOTÓN CORAZÓN PARA VOLVER A EMPEZAR
botonX = width - 110; // posición horizontal del botón en la esquina derecha
botonY = height - 75; // posición vertical del botón abajo
tamBoton = 38; // tamaño del botón corazón
let d = dist(mouseX, mouseY, botonX, botonY); // mide distancia entre mouse y botón
let pulso = 1 + sin(frameCount * 0.08) * 0.08; // crea efecto de latido
if (d &lt; tamBoton) {
fill(255, 0, 0); // rojo brillante si el mouse está encima
} else {
fill(180, 0, 0); // rojo oscuro si el mouse está lejos
}
textSize(16);
text(&quot;Volver a empezar&quot;, botonX, botonY - 45);
imageMode(CENTER);
image(corazon, botonX, botonY, tamBoton * 2 * pulso, tamBoton * 2.5 * pulso);
imageMode(CORNER);
}
// FUNCIONES PROPIAS
// Función para mostrar los segundos restantes en pantalla
function dibujarContador(tiempo) {
let segundosRestantes = ceil((duracionEstado - tiempo) / 1000);
textSize(16);
text(&quot;Cambiando en: &quot; + segundosRestantes + &quot; segundos...&quot;, width / 2, height / 2 + 20);
}
// Funcion para generar sombras
function dibujarSombras() {
for (let i = 0; i &lt; 15; i++) {

fill(0, 0, 0, 30); // cambia el color de relleno de la ellipse
ellipse(random(width), random(height), random(50,150));// sombras aleatorias
}
}
// Función para reiniciar todo el proyecto
function reiniciarJuego() {
estado = 0; // vuelve a la pantalla inicial
tiempoInicioEstado = millis(); // reinicia el temporizador
tamanoCirculo = 120; // devuelve el corazón a su tamaño original
floresExtras = []; // borra las flores sembradas
mensaje = &quot;Silencio&quot;; // reinicia el mensaje inicial
if (musica.isPlaying()) {
musica.stop(); // detiene la música
}
}
// INTERACCIONES
// Interaccion 1
function mousePressed() {
if (estado === 2) {
tamanoCirculo += 15; // agranda el corazón en la pantalla uno
background(255,0,0);
}
if (estado === 5) {
let d = dist(mouseX, mouseY, botonX, botonY); // calcula si el click fue sobre el botón
if (d &lt; tamBoton) {
reiniciarJuego(); // vuelve al inicio si presiona el corazón
} else {
floresExtras.push({
x: mouseX,
y: mouseY
}); // si no presiona el corazón, siembra una flor
}
}
}
// Interaccion 2
function keyPressed() {
if (estado === 0 &amp;&amp; key === &#39; &#39;) {
estado = 1;

tiempoInicioEstado = millis();
}
if (estado === 1 &amp;&amp; keyCode === ENTER) {
estado = 2;
tiempoInicioEstado = millis();
}
if (key === &#39;R&#39; || key === &#39;r&#39;) {
estado = 0;
tiempoInicioEstado = millis();
tamanoCirculo = 120;
}
if (estado === 4 &amp;&amp; keyCode === ENTER) {
estado = 5;
tiempoInicioEstado = millis();
}
}
// RESPONSIVE
function windowResized() {
resizeCanvas(windowWidth, windowHeight);}
}
```